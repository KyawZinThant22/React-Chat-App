{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider node backend/index.js",
    "server": "NODE_OPTIONS=--openssl-legacy-provider nodemon backend/index.js",
    "build": "NODE_OPTIONS=--openssl-legacy-provider npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix frontend && npm run build --prefix frontend"
  },  
  "keywords": [
    "mern",
    "chat-app"
  ],
  "author": "Kyaw Zin Thant",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "nodemon": "^3.0.2",
    "ws": "^8.16.0"
  }
}
